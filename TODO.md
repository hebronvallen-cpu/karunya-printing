# TODO
- [x] Jalankan migrasi `php artisan migrate --force` untuk membuat tabel `site_settings`.
- [x] Verifikasi status migrasi via `php artisan migrate:status`.
- [x] Bersihkan cache config/app via `php artisan config:clear` dan `php artisan cache:clear`.
- [x] Perbaiki tampilan admin settings: field `social_instagram/facebook/tiktok` sekarang untuk **link media sosial**, bukan teks pesan otomatis.

